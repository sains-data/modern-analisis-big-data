# Salin ke alert_rules.yml (gabungkan di bawah rules yang ada) — Latihan 5 Tugas B
#   - alert: DiskPredictedFull
#     expr: predict_linear(node_filesystem_avail_bytes{fstype!="tmpfs"}[1h], 4*3600) < 0
#     for: 10m
#     labels:
#       severity: warning
#     annotations:
#       summary: "Disk diprediksi penuh dalam 4 jam"
#
#   - alert: NetworkTrafficTinggi
#     expr: rate(node_network_receive_bytes_total[5m]) > 50 * 1024 * 1024
#     for: 5m
#     labels:
#       severity: warning
#     annotations:
#       summary: "Network receive > 50 MB/s"
